import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jdb.cuentas',
  appName: 'Cuentas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
